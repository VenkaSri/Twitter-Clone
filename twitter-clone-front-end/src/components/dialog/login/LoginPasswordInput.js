import React from "react";
import { useSelector } from "react-redux";
import { ContentBody } from "../../signup/dialog/ContentBody";

export const LoginPasswordInput = ({ userExists }) => {
  return (
    <>
      <ContentBody />
    </>
  );
};
